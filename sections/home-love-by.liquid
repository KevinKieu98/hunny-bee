{%  if section.settings.enable_section_love_by %}
<div class="{{ section.class }}">
    <div class="container">
        <div class="home_loved_by">
            {% if section.settings.title != blank %}
                <h2>{{ section.settings.title }}</h2>
            {% endif %}

            {% if section.blocks.size > 0 %}
                <div class="list_loved_by">
                    {% for block in section.blocks %}
                        {% case block.type %}
                            {% when 'image' %}
                                <div class="love_by_item">
                                    {% if block.settings.image_desktop != blank %}
                                        <img src="{{ block.settings.image_desktop | img_url: 'master' }}" alt="" loading="lazy" width="{{ block.settings.image_desktop.width }}"
                                            height="{{ block.settings.image_desktop.height }}" class="block w-full h-full image_desktop">
                                    {% else %}
                                        {{ 'lifestyle-2' | placeholder_svg_tag }}
                                    {% endif %}

                                    {% if block.settings.image_mobile != blank %}
                                        <img src="{{ block.settings.image_mobile | img_url: 'master' }}" alt="" loading="lazy" width="{{ block.settings.image_mobile.width }}"
                                            height="{{ block.settings.image_mobile.height }}" class="block w-full h-full image_mobile">
                                    {% else %}
                                        {{ 'lifestyle-2' | placeholder_svg_tag }}
                                    {% endif %}
                                </div>
                        {% endcase %}
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
</div>

{% endif %}
{% schema %}
    {
        "name": "Home Loved",
        "class": "home-loved",
        "settings": [
          {
                "type": "checkbox",
                "id": "enable_section_love_by",
                "label": "Enable?",
                "default": true
            },
            {
                "type": "text",
                "id": "title",
                "label": "Title",
                "default": "Loved by"
            }
          
        ],
        "blocks": [
            {
                "type": "image",
                "name": "Logo Image",
                "settings": [
                    {
                        "type": "image_picker",
                        "id": "image_desktop",
                        "label": "Image Desktop"
                    },
                    {
                        "type": "image_picker",
                        "id": "image_mobile",
                        "label": "Image Mobile"
                    }
                ]
            }
        ],
        "presets": [
            {
                "name": "Home Loved"
            }
        ]
    }
{% endschema %}
